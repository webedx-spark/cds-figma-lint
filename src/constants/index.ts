import { LintSettings } from '../types';

export const defaultSettings: LintSettings = {
  lintFillStyles: true,
  lintStrokeStyles: true,
  lintEffectStyles: true,
  lintTypoStyles: true,
  borderRadius: [0, 2, 4, 8, 16, 24, 32],
};
